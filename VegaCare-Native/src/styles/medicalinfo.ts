import { StyleSheet, Dimensions } from "react-native"

const {height } = Dimensions.get("screen")
const medicalinfo = StyleSheet.create({
    container :{
        width : "100%" , 
        height,
        backgroundColor:"white"
        
    },

})

export default medicalinfo